[package]
name = "diretta-sys"
version = "0.1.0"
edition = "2021"
authors = ["SPlayer Team"]
description = "Native C++ FFI bindings to Diretta Host SDK"

[dependencies]
thiserror = "2.0"

[build-dependencies]
cc = { version = "1.0", features = ["parallel"] }
